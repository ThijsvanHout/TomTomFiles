@model IEnumerable<TomToFileInfo.Models.Jam>

@{
    ViewBag.Title = "Index";
}

<h2>Index Test 8</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Region.RegionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Traject.TrajectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JamText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Region.RegionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Traject.TrajectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JamText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JamID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JamID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JamID })
        </td>
    </tr>
}

</table>
